# BITÁCORA DE EVOLUCIÓN Y CAMBIOS DEL PROYECTO

> **REGLA PARA ASISTENTES IA Y DESARROLLADORES:** Este documento es dinámico y obligatorio. Cada vez que se realice un cambio de arquitectura, refactorización, eliminación de código muerto, actualización de librerías o corrección de bugs, se DEBE agregar una entrada a este archivo antes de cerrar la tarea.

## Registro Histórico de Cambios
| Fecha (YYYY-MM-DD) | Módulo / Archivo | Tipo de Cambio (Fix/Refactor/Feature/Docs) | Resumen del Cambio | Impacto / Dependencias Afectadas |
|---|---|---|---|---|
| 2026-09-06 | Repositorio Completo | Docs | Inicio de auditoría técnica y creación de documentación viva (`CONTEXTO.md`, `Bitacora.md`) | Generación de `CONTEXTO.md` y `Bitacora.md` para uso por modelos IA y desarrolladores |
| 2026-09-06 | RAG / Seguridad / Frontend | Docs/Audit | Auditoría Fase 2: mapeo RAG, revisión de código muerto frontend y evaluación de seguridad/resiliencia | Identifica riesgos: credenciales en repo, timeouts inexistentes, riesgo de prompt injection, dependencias frontend no usadas |
| 2026-09-06 | Seguridad / Infraestructura | Fix | Limpieza inmediata: remoción del archivo de credenciales del árbol de trabajo, añadido de `credentials/` y `data/` a .gitignore, eliminación de `firebase` y `firebase-admin` de `package.json` | Credenciales movidas fuera del repo; dependencias eliminadas del manifest; se recomienda rotación y purgado del historial |
| 2026-09-06 | Auditoría Fase 1 | Docs/Validation | Fase 1 validada y operativa tras smoke tests: RAG en http://0.0.0.0:11400, backend Node arrancando, y persistencia SQLite en `./data/dev.sqlite` | Cambios aplicados: timeouts, num_ctx, persistencia local; pendiente saneamiento y modernización RAG |
| 2026-09-06 | Testing / Tests/ | Docs | Añadida `METODOLOGIA_TESTING.md` y estructura `tests/` (dataset y plantillas de iteración) para institucionalizar protocoles de evaluación y experimentación | Permite ejecutar benchmarks y documentar iteraciones experimentales; próximo: correr baseline y llenar `resultado_1.0.md` |
| 2026-09-17 | `tests/dataset/` | Fix/Data | Banco de preguntas ampliado a 100 (50 respondibles / 50 sin respaldo) y ground truth corregido: 24 de 52 preguntas citaban archivos fuera de `docs/sii/`, 11 de ellos documentos financieros de la CMF ajenos al SII | Auditado con revisión propia + validación cruzada con NotebookLM (99% concordancia). 21 reclasificadas, 18 eliminadas, 18 creadas. Rollback: `git revert` del commit; el banco previo queda en el historial |
| 2026-09-17 | `ai-service/ingest.py` | Fix | Modelo de embeddings fijado a `all-MiniLM-L6-v2`; antes intentaba `nomic-embed-text` (768 dims) con respaldo silencioso a MiniLM (384) mientras `api.py` usaba siempre MiniLM | Elimina riesgo de divergencia índice/consulta. **Requiere re-ingesta**: `cd ai-service && python ingest.py`. Rollback: revertir el commit y re-ingestar |
| 2026-09-17 | `ai-service/ingest.py` | Fix | La ingesta recrea la colección antes de insertar; antes `collection.add()` acumulaba y reindexar duplicaba los 48 fragmentos | Hace la ingesta idempotente. Resuelve la sospecha histórica de duplicados en ChromaDB |
| 2026-09-17 | `.gitignore`, `ai-service/chroma_db/` | Refactor | `chroma_db` y `__pycache__` salen del control de versiones | **Tras hacer pull hay que ejecutar `python ingest.py` una vez** para reconstruir el índice local; ya no viene en el repo |
| 2026-09-17 | `ai-service/docs/sii/inicio_actividades_sii.md` | Fix | Corregida una palabra en cirílico («независимо») que contaminaba el vector de ese fragmento | Requiere re-ingesta para propagarse al índice |
| 2026-09-17 | `scripts/` | Feature | Añadidos `evaluar_banco.py` (pipeline completo, separa fallos de recuperación de fallos de generación) y `medir_retrieval.py` (recall sin invocar al modelo) | El arnés importa el system prompt desde `api.py` en vez de copiarlo, para no medir una versión divergente |
| 2026-09-17 | `ai-service/api.py` | Docs | Experimento fallido: mover la regla de abstención tras el contexto subió la alucinación de 34% a 78%. Revertido | Documentado en `tests/iteraciones/experimento_prompt_v2.md`. `api.py` queda en el estado del baseline v1 |
| 2026-09-17 | `tests/iteraciones/` | Docs | Baseline 1.0 medido y planes reordenados según evidencia: modelo de generación a prioridad alta, chunking a media | De 31 fallos, 22 son de generación y 9 de recuperación |
| 2026-09-22 | `README.md` | Docs | Reescrito por completo: describía un proyecto anterior ("Krrete-BackEnd" con Firebase Auth, eliminado el 2026-09-06) y citaba un archivo `PROJECT_OVERVIEW.mb` inexistente. Ahora documenta la arquitectura real (Ollama + ai-service FastAPI + bot Node), instalación, variables de entorno y cómo evaluar el RAG | Sin impacto funcional; corrige documentación que llevaba desde mayo sin reflejar el pivot del proyecto |
| 2026-09-23 | `ai-service/api.py`, `scripts/evaluar_banco.py` | Feature | Pipeline de discriminación en dos pasos (iteración 1.6): un juez binario decide si el contexto contiene la respuesta y solo entonces se invoca al redactor. Añadidos `_build_judge_prompt`, `JUEZ_PROMPT_BASES` (variantes `estricto`/`flexible`) y la constante `FRASE_ABSTENCION`, más las flags `--dos-pasos`, `--juez`, `--redactor`, `--juez-prompt` y `--limite` en el arnés | **La alucinación baja de 34% a 0%** con `llama3.2` (3B) en ambos roles, en 19,6 min. `api.py` conserva el endpoint `/chat` de un paso: el pipeline de dos pasos vive por ahora solo en el arnés de evaluación. Ver `tests/iteraciones/iteracion_1.6_dos_pasos/resultado_1.6.md` |
| 2026-09-23 | `tests/iteraciones/iteracion_1.6_dos_pasos/` | Docs | Refutadas dos hipótesis sobre los falsos negativos del juez (recalibrar el prompt: recupera 1 de 9; juzgar fragmento por fragmento: recupera 2 de 9) y corregido el diagnóstico: la verificación estricta reduce los falsos negativos reales de 9 a 5 | Reordena la prioridad: **el cuello de botella es el chunking, no el juez** (19 de 25 abstenciones indebidas vienen de retrieval y chunking). OP-1 pasa a prioridad alta |
| 2026-09-23 | Raíz del repositorio | Refactor/Docs | El mapa de líneas de investigación sale de `tests/iteraciones/iteracion_1.0_baseline/analisis_y_bifurcaciones.md` y pasa a `ESTADO_INVESTIGACION.md` en la raíz, actualizado con el estado de las 7 líneas y una sección nueva de callejones sin salida ya medidos. Añadido `CLAUDE.md` con las reglas de trabajo del proyecto | **Corrige un problema estructural:** el mapa estaba atrapado en la carpeta de la 1.0 y solo llegaba a una rama al fusionar ese PR, así que las ramas 1.6 y posteriores citaban oportunidades (OP-6) inexistentes en su árbol. Al vivir en la raíz y fusionarse a `main` al cerrar cada iteración, queda disponible en todas las ramas y evita que se repitan experimentos ya refutados. Enlaces actualizados en `README.md`, `METODOLOGIA_TESTING.md`, `plan_1.3.md` y `plan_1.6.md` |
| 2026-09-23 | `CONTEXTO.md` | Docs | Reescrito y verificado línea por línea contra el código. Corregía: `nomic-embed-text` como modelo de ingesta (es `all-MiniLM-L6-v2` desde el 17), `num_ctx` y timeouts como «no detectados» (son 4096 y 60 s), métrica de similitud, credenciales y dependencias de Firebase ya eliminadas. Añadidos el inventario de código muerto, la tabla de deuda técnica abierta y el diagnóstico medido del chunking | **Hallazgo nuevo durante la verificación: 30 de 48 fragmentos (62%) del índice empiezan a mitad de frase.** La causa no es la falta de división estructural —`_chunk_text` ya divide por encabezados markdown— sino el solape por rebanada cruda de caracteres (`chunk[-150:]`), que antepone a cada fragmento una cola arrancada de otra sección. Precisa el objetivo de OP-1 y descarta una propuesta que habría sido retrabajo |
| 2026-09-23 | `ai-service/ingest.py`, `scripts/` | Fix/Feature | Iteración 1.1: `CHUNK_SIZE` 800 → 1400 y `CHUNK_OVERLAP` 150 → 200. Añadida la métrica `anclaje@k` a `medir_retrieval.py` (mide si la cita del ground truth llega íntegra al fragmento, no si llegó el archivo) y el script `ablacion_chunking.py` | **Primera iteración que cumple su criterio de éxito.** Abstención indebida 25 → 21, cobertura 47% → 57%, recall@6 88% → 92%, alucinación se mantiene en 0% y la especificidad del juez en 50/50. **Requiere re-ingesta**: `cd ai-service && python ingest.py`. Costo: el juez pasa de 6,4 s a 12,3 s por pregunta y la corrida de 19,6 a 28,2 min. El tope de 1400 está acoplado al truncado de `api.py`: subir uno exige subir el otro. Rollback: revertir las dos constantes y re-ingestar |
| 2026-09-23 | `tests/iteraciones/iteracion_1.1_chunking/` | Docs | Refutadas dos hipótesis sobre el chunking: la división estructural por sección markdown empeora el anclaje de 22/36 a 18/36, y quitar el solape es neutro a k=6 y negativo a k=10 | Corrige el diagnóstico de la 1.6, que señalaba al solape crudo como causa. La causa real era el **tamaño** del fragmento. `_chunk_text` ya dividía por encabezados markdown desde antes |
| 2026-09-23 | `scripts/medir_retrieval.py`, `scripts/ablacion_chunking.py` | Fix | El normalizador de `anclaje@k` no ignoraba los marcadores de lista markdown | El techo de la métrica pasa de 36 a 37 y la mejora de la 1.1 queda en 23 → 25, no 22 → 25. Aparece además que `CHUNK_OVERLAP=150` rinde más que el 200 adoptado (27/37 contra 25/37 con k=6): la elección se hizo con el normalizador erróneo. Pendiente de validar end-to-end |
| 2026-09-24 | `ai-service/docs/sii_piloto/`, `tests/iteraciones/corpus_esquema_piloto/` | Feature/Docs | Piloto del esquema estructurado del corpus propuesto por Yami (institución, requisitos previos, de qué trata, siguiente paso + pertenencia a un nodo del grafo), aplicado a 2 de los 13 documentos en un corpus paralelo | **Media confirmación.** Mejora el retrieval de forma atribuible: PREG-063 y PREG-064 pasan de estar fuera del top-6 a las posiciones 4 y 1, sin mover ninguna otra pregunta. **No mueve al juez**: ninguna de las 4 preguntas objetivo cambia de veredicto. El corpus vigente no se tocó; `ablacion_chunking.py` acepta `--docs` para comparar corpus alternativos |
| 2026-09-24 | `tests/iteraciones/iteracion_1.7_nodos/plan_1.7.md` | Docs | Plan de la iteración 1.7 (RAG basado en nodos), acordado como siguiente paso | OP-7 pasa de exploratoria a siguiente: el piloto mostró que las aristas del grafo se pueden **declarar** en el documento en vez de extraerse, que era lo que hacía cara esta línea. Techo medido: con el dato íntegro en el contexto el juez aprueba 21 de 25 (84%) |

## Deuda Técnica Inicial Detectada (Línea Base)
 - [x] Eliminar `credentials/*.json` del repositorio, rotar claves y usar un secret manager (alto riesgo de exposición).
 - [x] Revisar y remover dependencias no usadas (`firebase`, `firebase-admin`) si no son requeridas por backend.
 - [ ] Implementar sanitización y límites en el contenido recuperado para mitigar Prompt Injection (escapar instrucciones en `.md`).
 - [ ] Definir y documentar la métrica de similitud utilizada por ChromaDB o crear la colección con parámetros explícitos (actualmente: [NO DETECTADO EN EL REPOSITORIO]).
 - [x] Persistencia local en desarrollo: cambiar fallback SQLite in-memory por archivo `./data/dev.sqlite`.
 - [x] Añadir timeouts en llamadas RAG/Ollama y establecer `num_ctx` inicial en 4096.
 - [x] ~~**Nuevo 2026-09-17 — prioridad alta:** sustituir `llama3.2` (3B) por un modelo de 7–8B.~~ **Descartado 2026-09-23.** Escalar el modelo no resuelve la alucinación: `llama3.1:8b` la deja en 32% y `qwen2.5:7b` la sube a 42% (iteración 1.3). La arquitectura de dos pasos sí la lleva a 0% manteniendo el modelo de 3B (iteración 1.6). La línea de trabajo pasa a ser **hacer rendir al modelo pequeño cambiando la arquitectura**, no sustituirlo: un sistema que corre en hardware modesto es el objetivo, no una restricción.
 - [ ] Implementar el Roadmap de Refactorización (Fase 1-3): limpieza frontend, robustecimiento operativo y modernización del RAG; crear tickets/PRs y registrar avances en la bitácora.

## Backlog Prioritario (post-Fase1)
- [ ] Sanitización de fragments recuperados antes de su inclusión en el `system` prompt (mitigación de Prompt Injection). **Prioridad baja 2026-09-17**: sin casos observados en el baseline; el corpus es de elaboración propia. Medida preventiva para cuando se ingesten documentos de terceros.
- [ ] Modernización del chunking a token-based (usar tokenizer del modelo) y evaluación/benchmark de vectorstore local (ChromaDB vs Qdrant/Faiss). **Reprioritizado 2026-09-23 a alta**: el 88% de recall@6 del baseline se mide a nivel de *archivo* y es optimista; la iteración 1.6 mostró que a nivel de *chunk* el retrieval es bastante peor, y que el corte por caracteres entrega datos mutilados (caso PREG-076). El techo de 9 preguntas estimado en la 1.0 se calculó con la métrica laxa y es mayor. Ver `tests/iteraciones/iteracion_1.1_chunking/plan_1.1.md`.

## Instrucciones para elaborar nuevas entradas
- Cada entrada nueva debe contener: Fecha (YYYY-MM-DD), Módulo/Archivo afectado, Tipo de Cambio, Resumen corto, Impacto y Dependencias.
- Antes de cerrar cualquier PR que modifique la arquitectura o migraciones de datos, agregue una línea en esta bitácora describiendo la modificación y los pasos de rollback si aplica.

---

Archivo generado automáticamente el 2026-09-06 como parte de la Fase 1 de auditoría.
